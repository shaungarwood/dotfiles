# TMUX stuff
if [ "$TMUX" = "" ]; then tmux; fi

# General ZSH stuff
HISTFILE=~/.histfile
HISTSIZE=10000
SAVEHIST=10000
EDITOR='nvim'
path+=('/home/csw/bin')
source "$HOME/.aliases"

# Oh My ZSH stuff
export ZSH="$HOME/.oh-my-zsh"
ZSH_THEME="robbyrussell"
plugins=(git)
source $ZSH/oh-my-zsh.sh
