Send the user a push notification via their local `ntfy` script.

Run this bash command:

```bash
~/bin/ntfy "$ARGUMENTS"
```

If `$ARGUMENTS` is empty, use `"Done!"` as the message.

After running, confirm to the user that the notification was sent.
